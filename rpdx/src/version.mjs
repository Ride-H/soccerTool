/* =========================================================================
   RPDX.VERSION — ツールのバージョン（単一の真実源）
   ---------------------------------------------------------------------------
   main へのリリースごとにここを更新し、Git タグ（v<major.minor.patch>）・
   GitHub Release と一致させる。UI（ローディング画面・試合情報フッタ）に表示する。
   ========================================================================= */
(() => {
  const R = (globalThis.RPDX ??= {});
  R.VERSION = "1.1.0";
})();
