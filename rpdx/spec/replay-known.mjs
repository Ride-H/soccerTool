// リプレイ評価で「既に分かっている破綻」の台帳。
//
// 新しく出た破綻だけを失敗にするための一覧。ここに載せることは「直さない」ではなく
// 「原因が特定済みで、対応 Issue に紐づいている」という意味。reason には必ず
// 何が起きているか（症状ではなく原因）を書く。直したら行ごと消す。
export const KNOWN = [
  {
    id: "KN-03", pattern: /が 1[0-3]\.\d+m\/s（上限 9\.9）/, at: ["プレス順位の入れ替わり"],
    reason: "最近接プレッサーの順位（1st/2nd/3rd）が距離で毎フレーム決まり、入れ替わった瞬間に "
      + "寄せる距離（0.9m/1.6m）と重み（0.52/0.32/0.22）が離散的に切り替わる。"
      + "目標が飛ぶので選手が 1 フレームで数メートル動く。",
    issue: 178, note: "2026-07-31。#173/#174 の修正後に残った唯一の型（16 件・最大 13.4m/s）",
  },
];

// 破綻メッセージが既知かどうか（既知なら失敗に数えない）
export const isKnown = (msg) => KNOWN.find((k) => k.pattern.test(msg)) || null;
