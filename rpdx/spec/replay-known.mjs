// リプレイ評価で「既に分かっている破綻」の台帳。
//
// 新しく出た破綻だけを失敗にするための一覧。ここに載せることは「直さない」ではなく
// 「原因が特定済みで、対応 Issue に紐づいている」という意味。reason には必ず
// 何が起きているか（症状ではなく原因）を書く。直したら行ごと消す。
export const KNOWN = [
  {
    id: "KN-04", pattern: /が 1[0-1]\.\d+m\/s（上限 9\.9）/, at: ["リスタート直後", "寄せの解放"],
    reason: "#178 で順位の連続化と解放速度の制限を入れて 18 件→2 件・13.4→11.0m/s まで下げた残り。"
      + "延長のキックオフ直後（再整列からの復帰）と、寄せの解放が他の項と重なる場面で 11% ほど超える。"
      + "寄せの解放に割ける速度（PRESS_V）をこれ以上下げるとプレスの較正テストが落ちるため、"
      + "残りは「複数の項の合成をまとめて上限内に収める」設計側の課題。",
    issue: 178, note: "2026-07-31。残り 2 件（arg-egy EGY#12 10.64 / final ESP#7 11.04）",
  },
];

// 破綻メッセージが既知かどうか（既知なら失敗に数えない）
export const isKnown = (msg) => KNOWN.find((k) => k.pattern.test(msg)) || null;
