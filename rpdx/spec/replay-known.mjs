// リプレイ評価で「既に分かっている破綻」の台帳。
//
// 新しく出た破綻だけを失敗にするための一覧。ここに載せることは「直さない」ではなく
// 「原因が特定済みで、対応 Issue に紐づいている」という意味。reason には必ず
// 何が起きているか（症状ではなく原因）を書く。直したら行ごと消す。
export const KNOWN = [
  {
    id: "KN-01", pattern: /が (1[2-9]|[2-9]\d)\.\d+m\/s（上限 9\.9）/, at: ["交代", "退場"],
    reason: "交代・退場の瞬間にチーム全員のスロットが割り当て直され、残っている選手が最大 36m 飛ぶ。"
      + "位置は f(t) の純関数なので次フレームには正しい場所に居るが、その 1 フレームだけ速度上限を破る。",
    issue: 173, note: "2026-07-31 リプレイ評価で発見",
  },
  {
    id: "KN-02", pattern: /が (9\.9|10\.|11\.)\d*m\/s（上限 9\.9）/, at: ["記録イベントの直前"],
    reason: "記録イベント（シュート等）のアンカーへ収束させる区間で、上限を 1〜4% 超える。"
      + "帯域制限ノイズ側の上限保証はアンカー補間には掛かっていない。",
    issue: 174, note: "2026-07-31 リプレイ評価で発見",
  },
];

// 破綻メッセージが既知かどうか（既知なら失敗に数えない）
export const isKnown = (msg) => KNOWN.find((k) => k.pattern.test(msg)) || null;
